<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- TV App 主主题 -->
    <style name="Theme.CalctoolTV" parent="Theme.Leanback">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorPrimary">@color/brand</item>
        <item name="colorPrimaryDark">@color/brand_dark</item>
        <item name="colorAccent">@color/brand</item>
    </style>

    <!-- Splash 主题：用 AppCompat 基类，HarmonyOS 兼容性更好 -->
    <style name="Theme.CalctoolTV.Splash" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- 播放器全屏主题 -->
    <style name="Theme.CalctoolTV.Player" parent="Theme.CalctoolTV">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
